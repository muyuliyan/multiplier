/* This is just a mind code, rather than a real C code. */

#include<stdio.h>

int main(){
    int x[32], y[32];
    int result[64];
    
    int temp_x[64] = (sign*32, x[]);
    int temp_y[64] = (sign*32, y[]);
    int box[128];

    int count = 0;
    for(;count < 64; count ++){
        if(temp_y[count] == 1){
            box = box + x<<count;
        }
    }

    for(;count < 32; count++){
        result[count] = box[count];
    }

    return result[];
}